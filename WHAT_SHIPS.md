# WHAT_SHIPS — Hathor Red live capability snapshot

Last updated: 2026-09-11 (dose-1.42 roomController path :id uses toPositiveInt).

## Ships today

- **Auth**: email/password + JWT. No OAuth routes mounted. `authMiddleware` normalizes `req.user` to `{ userId, username }` (same shape as `streamAuth`) and rejects stream-typed tokens on the Bearer path. **dose-1.27**: `authMiddleware` requires finite positive integer `userId` (reject NaN/0/negative/non-integer before `req.user` is set), matching the stream-token bar from dose-1.26. **dose-1.38**: `authMiddleware` uses shared `toPositiveInt` from `streamToken` (DRY with streamAuth path/Bearer checks). **dose-1.39**: Socket.IO handshake uses the same bar (reject `typ === 'stream'`, require positive-int `userId` before `socket.userId` is set).
- **Playback (Dose 1 core)**: signed stream URLs (`GET /api/songs/:id/stream-url` → `/stream?t=…`) so HTML5 `<audio src>` works without Authorization headers. `streamAuth` + `streamToken` (short-lived, song-scoped). streamAuth normalizes `req.user` to `{ userId, username }`, rejects stream-typed tokens on the Bearer path, and requires `songId`/`userId` on query tokens. **dose-1.24**: stream tokens optionally carry `username` from the minting session so `req.user.username` is populated on the query-token path without a DB lookup. **dose-1.26**: `signStreamToken` / `verifyStreamToken` require finite positive integer `userId` and `songId` (reject NaN/0/negative/string junk before `req.user` is set). **dose-1.28**: when `?t=` is present and the route has `:id`, `streamAuth` rejects the request if `token.songId` does not match the path param (defense in depth; `streamSong` still re-checks). **dose-1.29**: `getStreamUrl` and `streamSong` call `toPositiveInt` on path `:id` before DB or token mint and return 400 on invalid ids (same bar as tokens; independent of validation middleware order). **dose-1.30**: `getSongById` also applies `toPositiveInt` on path `:id` and returns 400 on invalid ids (same bar as stream endpoints). **dose-1.31**: `recordListening` applies `toPositiveInt` on body `songId` (and non-negative integer duration) before DB insert (same bar; independent of validation middleware). **dose-1.32**: `updatePlaybackState` applies `toPositiveInt` on body `currentSongId` when present and non-null (null still clears; same bar before DB upsert). **dose-1.33**: `updatePlaybackState` applies bounded finite checks on `position` (0–7200), `volume` (0–1), and `playbackSpeed` (0.25–4) when present (reject NaN/Infinity/out-of-range before DB upsert; omitted keys still leave prior values). **dose-1.34**: `updatePlaybackState` requires strict boolean `isPlaying` when present (reject string/number/null); `recordListening` bounds `duration` to 0–7200 (same ceiling as position). **dose-1.35**: `updatePlaybackState` bounds `pitchShift` to finite [-24, 24] when present (explicit null clears) and requires `stemsConfig` to be null or a plain object (reject arrays/primitives); pitch/stems UI still does not ship. **dose-1.36**: `updatePlaybackState` uses CASE flags for `pitch_shift` / `stems_config` so explicit null actually writes NULL (COALESCE could not clear); validation helpers return `'INVALID'` for bad present values and omit the CASE flag when the key is absent. **dose-1.37**: `streamAuth` path `:id` match and Bearer `userId` normalization both use shared `toPositiveInt` (DRY with token/controller bar). **dose-1.39**: socket `sync-state` applies the same positive-int / bounded / strict-boolean bar before DB upsert + Redis write (throttle 4/s); invalid payloads are dropped silently. **dose-1.40**: socket `room-control` uses shared `toPositiveInt` for `roomId`/`songId` and bounds `position` to finite [0, 7200] before DB update (same bar as sync-state / HTTP playback; invalid position/songId emits error and skips write). **dose-1.41**: remaining socket room handlers (`join-room`, `leave-room`, `request-room-state`, `room-reaction`, `room-chat`, RTC relay, `getRoomPresenceRoster`) use shared `toPositiveInt` for `roomId`/`targetUserId` (same bar as room-control; reject NaN/0/negative before presence or DB work). **dose-1.42**: HTTP `roomController` path handlers (`getRoomById`, `joinRoom`, `leaveRoom`, `deleteRoom`) apply shared `toPositiveInt` on `:id` and return 400 on invalid ids (same bar as socket room path; independent of validation middleware).
- **Player**: full `PlayerContext` on main (queue, Fisher-Yates shuffle, repeat, seek guards, play-generation, stream error retry preserves seek, volume/speed, hydrate from `/playback/state`, persist debounce). **Logout clear** (dose-1.20/1.21): when `isAuthenticated` becomes false, `clearQueue()` runs and `hydratedRef` resets so signed streams stop and a later login can hydrate again.
- **Queue UI**: up-next panel, drag reorder (linear only), play-next / makeNext / insertNext (shuffle-aware), dedupe on add.
- **Playlists / Home genre filter / Rooms / Olympus flag gates / Podcasts (soon)** as prior doses.
- **Pitch/stems**: not implemented; UI controls remain hidden.

## Does not ship

- HLS in the live player, OAuth, WebRTC rooms, stem/pitch, cross-device live queue list, podcast catalog.

## Dose status

| Dose | Status |
|------|--------|
| 0 Truth | Done |
| 1 Playback | Core done; streamAuth hardened (dose-1.23); stream token username (dose-1.24); authMiddleware normalize (dose-1.25); stream token positive-int ids (dose-1.26); authMiddleware positive-int userId (dose-1.27); streamAuth path songId match (dose-1.28); stream controller positive-int songId (dose-1.29); getSongById positive-int songId (dose-1.30); recordListening positive-int songId (dose-1.31); updatePlaybackState positive-int currentSongId (dose-1.32); updatePlaybackState bounded position/volume/playbackSpeed (dose-1.33); isPlaying strict boolean + recordListening duration 0–7200 (dose-1.34); pitchShift [-24,24] + stemsConfig plain-object (dose-1.35); explicit-null clear for pitch/stems via CASE (dose-1.36); streamAuth toPositiveInt DRY (dose-1.37); authMiddleware toPositiveInt DRY (dose-1.38); socket auth + sync-state same bar (dose-1.39); socket room-control same bar (dose-1.40); socket join/leave/chat/reaction/RTC/roster same bar (dose-1.41); roomController HTTP path ids same bar (dose-1.42) |
| 2 Account | Soft logout + profile path present |
| 3–5 | Routes/flags/rooms as prior |

## Next item

Optional multi-device live queue list (not claimed). Residual Dose 2 profile polish if regressions appear. Playlist path-id hardening (toPositiveInt) is a natural follow-on.

See also: [README.md](README.md), [BUGS.md](BUGS.md), [API.md](API.md).
